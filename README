* To use
  - create a mysql database, and a user with privleges on that database.
  - update the setting.py file with the new DB settings
  - modify WORKSPACE_DIR to point to your local git copy of step2 (the path shouldn't contain "step2")
  - run setup.sh script to install 3rd party apps
